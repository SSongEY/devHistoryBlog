# 글 작성 규약 — 공개 저장소 sanitize 게이트

이 블로그는 **공개 GitHub Pages**로 배포된다 (`https://SSongEY.github.io/devHistoryBlog/`).
저장소를 private로 두더라도 **발행된 Pages 사이트 자체는 인터넷에 공개**된다. 따라서
업무/학습 기록을 올릴 때 아래 규칙으로 비공개 정보를 반드시 걸러낸다.

## 커밋 전 셀프체크 (5문항)

글을 커밋하기 전에 아래를 스스로 확인한다. 하나라도 걸리면 일반화하거나 삭제한다.

1. **실제 파일 경로 / 함수명 / 라인번호**가 있나? → 제거하거나 일반적 설명으로 대체
2. **내부 저장소명 / 시스템 endpoint / 호스트명 / 프로젝트 코드명**이 있나? → 제거·일반화
3. **운영 파라미터 실측값**(임계치, 튜닝 상수 등)을 그대로 노출하나? → 예시값으로 바꾸고 "설명용 예시"라 명시
4. **실측 지표·매출·트래픽 수치 / 사번·이메일·이름 등 PII**가 있나? → 제거
5. **사내 문서(Wiki/Jira) 링크나 캡처**가 있나? → 제거

## 원칙 — "개념은 공개, 고유 조립은 비공개"

- **올려도 되는 것**: 공개 교과서 지식(알고리즘·수학·오픈소스 사용법), 일반화된 트러블슈팅 패턴, 스스로 학습한 개념 정리.
- **가려야 하는 것**: 특정 회사/서비스의 고유한 구현·튜닝·경로·수치·아키텍처 디테일.

같은 주제라도 *"Thompson Sampling으로 cold-start를 푸는 일반 원리"*는 공개 OK,
*"우리 서비스의 retriever가 파라미터 X=30으로 튜닝돼 있다"*는 비공개.

## draft로 미루기

확신이 안 서면 front-matter에 `draft: true`로 두면 배포에서 제외된다(로컬에서만 보임).
sanitize를 마친 뒤 `draft: false`로 바꿔 발행한다.
